@model ForgotPasswordViewModel
@{
    Layout = "/Views/Shared/_LayoutInitial.cshtml";
    ViewData["Title"] = "Recuperar Password";
}

<div class="row forgot-password-form">
    <div class="col-md-4">
        <form asp-action="ForgotPassword" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group has-feedback has-feedback-left">
                <label asp-for="Email"></label>
                <div class="input-group">
                    <input asp-for="Email" class="form-control" placeholder="exemplo@ips.pt" />
                    <i class="glyphicon glyphicon-envelope form-control-feedback"></i>
                    <div class="input-group-btn">
                        <button class="btn bottom-right pop" type="button"  >
                            <span class="glyphicon glyphicon-question-sign"></span>
                        </button>
                    </div>
                </div>
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <button type="submit" class="button btn btn-default">Submeter</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
